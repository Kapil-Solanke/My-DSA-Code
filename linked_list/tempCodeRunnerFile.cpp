while (org_head != NULL && clone_head != NULL)
    // {
    //     Node *org_random = org_head->arb;
    //     if (org_random != NULL)
    //     {
    //         clone_head->arb = org_head->arb->next;
    //         // clone_head->arb = org_random->next;
    //         org_head = clone_head->next;
    //         clone_head = org_head->next;
    //     }
    //     else
    //     {
    //         org_head = clone_head->next;
    //         clone_head = org_head->next;
    //     }
    // }